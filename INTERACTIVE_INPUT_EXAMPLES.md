# 🎯 交互式输入测试示例

## 测试新的 input() 功能

现在可以直接在代码编辑器中测试这些示例！

---

## 示例 1：基础用户输入（您的代码）

```python
# 获取用户输入
first_name = input("请输入名字: ")
last_name = input("请输入姓氏: ")
country = input("请输入国家: ")
age = input("请输入年龄: ")

print(f"姓名: {first_name} {last_name}")
print(f"国家: {country}")
print(f"年龄: {age}")
```

**运行时会弹出 4 个对话框：**
1. "请输入名字:" → 输入 "张三"
2. "请输入姓氏:" → 输入 "李"
3. "请输入国家:" → 输入 "中国"
4. "请输入年龄:" → 输入 "25"

**输出：**
```
姓名: 张三 李
国家: 中国
年龄: 25
```

---

## 示例 2：简单计算器

```python
# 计算器
num1 = float(input("输入第一个数字: "))
num2 = float(input("输入第二个数字: "))

print(f"加法: {num1} + {num2} = {num1 + num2}")
print(f"减法: {num1} - {num2} = {num1 - num2}")
print(f"乘法: {num1} × {num2} = {num1 * num2}")
print(f"除法: {num1} ÷ {num2} = {num1 / num2}")
```

**测试输入：**
- 第一个数字: 10
- 第二个数字: 5

**输出：**
```
加法: 10.0 + 5.0 = 15.0
减法: 10.0 - 5.0 = 5.0
乘法: 10.0 × 5.0 = 50.0
除法: 10.0 ÷ 5.0 = 2.0
```

---

## 示例 3：密码验证

```python
# 密码验证
username = input("请输入用户名: ")
password = input("请输入密码: ")

# 简单验证（仅作演示）
if username == "admin" and password == "12345":
    print("✅ 登录成功！")
    print(f"欢迎回来，{username}！")
else:
    print("❌ 用户名或密码错误")
```

**测试输入（成功）：**
- 用户名: admin
- 密码: 12345

**输出：**
```
✅ 登录成功！
欢迎回来，admin！
```

---

## 示例 4：购物清单

```python
# 购物清单
items = []
total_items = int(input("你想买多少件商品? "))

for i in range(total_items):
    item = input(f"请输入第 {i+1} 件商品名称: ")
    items.append(item)

print("\n📋 你的购物清单:")
for index, item in enumerate(items, 1):
    print(f"{index}. {item}")

print(f"\n总共 {len(items)} 件商品")
```

**测试输入：**
- 商品数量: 3
- 商品1: 苹果
- 商品2: 香蕉
- 商品3: 橙子

**输出：**
```
📋 你的购物清单:
1. 苹果
2. 香蕉
3. 橙子

总共 3 件商品
```

---

## 示例 5：温度转换

```python
# 温度转换器
print("=== 温度转换器 ===")
print("1. 摄氏度 → 华氏度")
print("2. 华氏度 → 摄氏度")

choice = input("请选择 (1/2): ")
temp = float(input("请输入温度: "))

if choice == "1":
    result = (temp * 9/5) + 32
    print(f"{temp}°C = {result:.2f}°F")
elif choice == "2":
    result = (temp - 32) * 5/9
    print(f"{temp}°F = {result:.2f}°C")
else:
    print("无效选择！")
```

**测试输入：**
- 选择: 1
- 温度: 25

**输出：**
```
=== 温度转换器 ===
1. 摄氏度 → 华氏度
2. 华氏度 → 摄氏度
25.0°C = 77.00°F
```

---

## 示例 6：猜数字游戏

```python
# 猜数字游戏
import random

secret_number = random.randint(1, 100)
attempts = 0
max_attempts = 7

print("🎮 猜数字游戏")
print(f"我想了一个 1-100 的数字，你有 {max_attempts} 次机会！")

while attempts < max_attempts:
    guess = int(input(f"\n第 {attempts + 1} 次猜测: "))
    attempts += 1
    
    if guess == secret_number:
        print(f"🎉 恭喜！你猜对了！")
        print(f"你用了 {attempts} 次就猜中了！")
        break
    elif guess < secret_number:
        print("📈 太小了，再试试！")
    else:
        print("📉 太大了，再试试！")
    
    if attempts == max_attempts:
        print(f"\n😢 游戏结束！正确答案是 {secret_number}")
```

**测试示例运行：**
```
🎮 猜数字游戏
我想了一个 1-100 的数字，你有 7 次机会！

第 1 次猜测: 50
📈 太小了，再试试！

第 2 次猜测: 75
📉 太大了，再试试！

第 3 次猜测: 62
🎉 恭喜！你猜对了！
你用了 3 次就猜中了！
```

---

## 示例 7：BMI 计算器

```python
# BMI 计算器
print("💪 BMI（身体质量指数）计算器")

weight = float(input("请输入你的体重 (kg): "))
height = float(input("请输入你的身高 (m): "))

bmi = weight / (height ** 2)

print(f"\n你的 BMI: {bmi:.2f}")

if bmi < 18.5:
    print("分类: 偏瘦")
elif 18.5 <= bmi < 24:
    print("分类: 正常")
elif 24 <= bmi < 28:
    print("分类: 偏胖")
else:
    print("分类: 肥胖")
```

**测试输入：**
- 体重: 70
- 身高: 1.75

**输出：**
```
💪 BMI（身体质量指数）计算器

你的 BMI: 22.86
分类: 正常
```

---

## 示例 8：菜单驱动程序

```python
# 简单菜单系统
while True:
    print("\n=== 主菜单 ===")
    print("1. 打招呼")
    print("2. 显示日期")
    print("3. 计算平方")
    print("4. 退出")
    
    choice = input("\n请选择 (1-4): ")
    
    if choice == "1":
        name = input("你的名字: ")
        print(f"你好, {name}! 很高兴见到你！")
    
    elif choice == "2":
        from datetime import datetime
        now = datetime.now()
        print(f"今天是: {now.strftime('%Y年%m月%d日')}")
    
    elif choice == "3":
        num = float(input("输入一个数字: "))
        print(f"{num} 的平方是 {num ** 2}")
    
    elif choice == "4":
        print("再见！👋")
        break
    
    else:
        print("❌ 无效选择，请重试")
```

---

## 🎯 测试建议

### 1. 从简单开始
先测试示例 1 和 2，熟悉弹窗输入的流程。

### 2. 尝试不同类型
测试字符串、整数、浮点数等不同类型的输入。

### 3. 测试边界情况
- 输入空值
- 点击取消
- 输入错误类型（如字母代替数字）

### 4. 体验交互性
示例 6 和 8 提供了良好的交互体验，可以感受真实的应用场景。

---

## ✅ 验证功能

复制任何一个示例到代码编辑器中，点击"运行代码"：

1. ✅ 等待 Python 环境就绪（右上角显示 ✓ 就绪）
2. ✅ 代码运行时会弹出输入对话框
3. ✅ 每个 input() 都会触发一次弹窗
4. ✅ 输入完成后查看输出结果

享受真正的交互式 Python 编程体验！🎉

